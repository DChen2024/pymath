'''
This project provides some basic math functions  
not part of the Python standard library.
'''

from typing import SupportsIndex

def ilog2(n: SupportsIndex, /) -> int:
    """Calculate integer base-2 logarithm of n, rounded down.
    #### Exceptions
    - Raises `ValueError` if argument is not positive"""

def icbrt(n: SupportsIndex, /) -> int:
    """Calculate integer cube root of n, rounded to 0."""
